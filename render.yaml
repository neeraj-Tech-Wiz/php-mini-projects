services:
  - type: web
    name: marksheet-app
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: true

databases:
  - name: marksheetdb
    plan: free
    databaseName: marksheetdb
    user: postgres
